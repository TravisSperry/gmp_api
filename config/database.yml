default: &default
  adapter: postgresql
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  timeout: 5000

development:
  <<: *default
  database: db/gmp_api_development

test:
  <<: *default
  database: db/gmp_api_test

production:
  <<: *default
  database: db/gmp_api_production
